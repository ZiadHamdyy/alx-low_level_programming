#include "main.h"
/**
 * mul - multiplies two integers.
 * @a:first number.
 * @b:second number.
 * Return: multiplies of the first and the second number.
 */
int mul(int a, int b)
{
	return (a * b);
}
