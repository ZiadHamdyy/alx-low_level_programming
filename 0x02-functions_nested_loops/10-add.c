#include "main.h"
/**
 * add - adds two integers and returns the result.
 * x = first value  y = second value
 * Return:the result of the sum of those two values.
 */
int add(int x, int y)
{
	int r;

	r = x + y;
	return (r);
}
